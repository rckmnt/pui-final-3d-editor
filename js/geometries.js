// Basic 3D dimensions and properties of each

var baseHeight, radius, leftArm, rightArm, length, angle;

const small = {
  baseHeight: 30,
  radius: 9.25,
  leftArm: {
    length: 30,
    angle: Math.PI / 6
  },
  rightArm: {
    length: 40,
    angle: -Math.PI / 6
  }
}

const large = {
  baseHeight: 70,
  radius: 9.25,
  leftArm: {
    length: 80,
    angle: Math.PI / 6
  },
  rightArm: {
    length: 80,
    angle: -Math.PI / 6
  }
}
